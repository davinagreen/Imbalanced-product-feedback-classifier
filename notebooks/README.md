# Notebooks

Original Jupyter notebooks from development live in `original/`. They may contain empty code cells (source cleared but outputs retained).

For reproducible runs, prefer the scripts in `../src/`:

- `baseline_logistic_regression.py` — logistic regression baseline
- `ensemble_voting.py` — soft voting ensemble (ADASYN / Borderline-SMOTE)
- `stacking_models.py` — grid search + stacking (SVM, XGBoost, etc.)
